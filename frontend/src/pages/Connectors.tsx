import React from 'react'
import Services from './Services'

// Reuse the Services page until connectors get its own backend endpoint
const Connectors: React.FC = () => {
  return <Services />
}

export default Connectors
